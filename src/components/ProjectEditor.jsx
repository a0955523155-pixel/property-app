import React, { useState, useMemo, useEffect } from 'react';
import { 
  Plus, Trash2, DollarSign, Image as ImageIcon, FileSpreadsheet, Printer, X, 
  Users, Map, Home, Edit2, Save, ArrowLeft, Check, Camera, 
  Calculator, Minus, Link as LinkIcon, ClipboardCheck, Key, FileText, Calendar as CalendarIcon, Tag, Building2, Settings, Briefcase, Filter, Eye, EyeOff
} from 'lucide-react';
import { CATEGORIES, PREDEFINED_SELLERS, toPing, createEmptyLandItem, exportMasterCSV } from '../utils/helpers';
import LinkedLedger from './LinkedLedger';

const deepSanitize = (data) => {
  if (data === undefined) return null;
  if (data === null || typeof data !== 'object') return data;
  if (Array.isArray(data)) return data.map(item => deepSanitize(item));
  const sanitizedObj = {};
  Object.keys(data).forEach(key => {
    const value = data[key];
    sanitizedObj[key] = (typeof value !== 'function' && typeof value !== 'symbol') ? deepSanitize(value) : null;
  });
  return sanitizedObj;
};

const ProjectEditor = ({ initialData, onSave, onBack, agencyDB }) => {
  if (!initialData) return null;

  const [activeTab, setActiveTab] = useState('finance'); 
  
  // 1. 專案基本狀態
  const [projectName, setProjectName] = useState(initialData.name || "新案件名稱");
  const [projectZone, setProjectZone] = useState(initialData.zone || "未分類"); 
  const [projectSite, setProjectSite] = useState(initialData.site || "大成工業城");
  const [isEditingName, setIsEditingName] = useState(false);

  // 專案團隊
  const [projectTeam, setProjectTeam] = useState(initialData.projectTeam || {
    agency: "", broker: "", 
    developer: "", developerType: "general", developerNo: "", developerImg: null, 
    marketer: "", marketerNo: "", marketerImg: null,
    scrivener: ""
  });

  const [printConfig, setPrintConfig] = useState({
    team: true, buyers: true, lands: true, buildings: true, handover: true, finance: true
  });
  const [showPrintSettings, setShowPrintSettings] = useState(false);

  // 2. 買受人
  const [buyers, setBuyers] = useState(initialData.buyers || []);
  const [newBuyer, setNewBuyer] = useState({ name: "", phone: "", address: "", image: null });
  const [editingBuyerId, setEditingBuyerId] = useState(null);

  // 3. 土地
  const [lands, setLands] = useState(initialData.lands || []);
  const [showLandForm, setShowLandForm] = useState(false);
  const [editingLandId, setEditingLandId] = useState(null);
  const [tempLand, setTempLand] = useState({ section: "", items: [createEmptyLandItem()], sellers: [] });
  const [tempLandSeller, setTempLandSeller] = useState({ name: "", phone: "", address: "" });

  // 4. 建物
  const [buildings, setBuildings] = useState(initialData.buildings || []);
  const [showBuildingForm, setShowBuildingForm] = useState(false);
  const [editingBuildingId, setEditingBuildingId] = useState(null);
  const [tempBuilding, setTempBuilding] = useState({
    permitNumber: "", address: "", license: "", buildNumber: "", areaM2: "", pricePerUnit: "", totalPrice: "", sellers: [],
    permitImage: null, licenseImage: null, buildNoImage: null
  });
  const [tempBuildingSeller, setTempBuildingSeller] = useState({ name: "", phone: "", address: "" });

  // 5. 財務帳目
  const [transactions, setTransactions] = useState(initialData.transactions || []);
  const [newTx, setNewTx] = useState({
    date: new Date().toISOString().split('T')[0], type: 'expense', category: CATEGORIES.expense[0], amount: '', note: '', image: null, linkedId: null, linkedType: 'general' 
  });
  const [editingTxId, setEditingTxId] = useState(null);
  const [previewImage, setPreviewImage] = useState(null);
  
  // 顯示開關
  const [visibleLedgers, setVisibleLedgers] = useState({
    general: true, land: true, building: true
  });

  // 6. 交屋
  const defaultHandover = {
    handoverDate: "", remotes: "0", keysFront: "0", keysBack: "0", warranty: false, drawings: false, electricityBill: "", waterBill: "", originalPermit: false
  };
  const [handoverData, setHandoverData] = useState({ ...defaultHandover, ...(initialData.handoverData || {}) });

  // --- 計算邏輯 ---
  const landGrandTotal = useMemo(() => {
    let totalAreaM2 = 0, totalAreaPing = 0, totalMoney = 0;
    lands.forEach(l => {
      totalAreaM2 += Number(l.holdingAreaM2) || 0;
      totalAreaPing += Number(l.holdingAreaPing) || 0;
      totalMoney += Number(l.totalPrice) || 0;
    });
    return { m2: totalAreaM2.toFixed(3), ping: totalAreaPing.toFixed(3), price: totalMoney };
  }, [lands]);

  const stats = useMemo(() => {
    let totalIncome = 0, totalExpense = 0;
    const subTotals = { general: { income: 0, expense: 0 }, land: { income: 0, expense: 0 }, building: { income: 0, expense: 0 } };
    transactions.forEach(t => {
      const val = Number(t.amount) || 0;
      const lType = t.linkedType || 'general';
      if (t.type === 'income') { totalIncome += val; if (subTotals[lType]) subTotals[lType].income += val; } 
      else { totalExpense += val; if (subTotals[lType]) subTotals[lType].expense += val; }
    });
    const netProfit = totalIncome - totalExpense;
    const roi = totalExpense > 0 ? ((netProfit / totalExpense) * 100).toFixed(2) : 0;
    return { totalIncome, totalExpense, netProfit, roi, subTotals };
  }, [transactions]);

  // 財務分組邏輯
  const groupedTransactions = useMemo(() => {
    const groups = { general: [], land: [], building: [] };
    const sortedTx = [...transactions].sort((a,b) => new Date(a.date) - new Date(b.date));
    sortedTx.forEach(t => {
      const type = t.linkedType || 'general';
      if (groups[type]) groups[type].push(t);
    });
    return groups;
  }, [transactions]);

  // --- 自動儲存 ---
  useEffect(() => {
    if (!initialData) return;
    document.title = projectName || "專案報表";
    const timer = setTimeout(() => {
      const rawData = { 
        id: initialData.id, name: projectName, zone: projectZone, site: projectSite, 
        projectTeam, buyers, lands, buildings, transactions, handoverData, updatedAt: new Date().toISOString() 
      };
      onSave(deepSanitize(rawData));
    }, 1500);
    return () => clearTimeout(timer);
  }, [projectName, projectZone, projectSite, projectTeam, buyers, lands, buildings, transactions, handoverData]);

  const handleImageUploadGeneric = (file, callback) => { if (file) { const reader = new FileReader(); reader.onloadend = () => callback(reader.result); reader.readAsDataURL(file); } };
  const handleImageUpload = (e) => { const file = e.target.files[0]; if (file) { const reader = new FileReader(); reader.onloadend = () => setNewTx({ ...newTx, image: reader.result }); reader.readAsDataURL(file); } };

  // 仲介公司選擇
  const handleAgencyChange = (agencyName) => {
    const brokers = (agencyDB && agencyDB[agencyName]) || [];
    const autoBroker = brokers.length === 1 ? brokers[0] : "";
    setProjectTeam({ ...projectTeam, agency: agencyName, broker: autoBroker });
  };

  // --- CRUD (略) ---
  const addLandSeller = () => { if (!tempLandSeller.name) return; setTempLand({ ...tempLand, sellers: [...tempLand.sellers, { id: Date.now(), ...tempLandSeller }] }); setTempLandSeller({ name: "", phone: "", address: "" }); };
  const removeLandSeller = (id) => { setTempLand({ ...tempLand, sellers: tempLand.sellers.filter(s => s.id !== id) }); };
  const addLandItemField = () => { setTempLand({ ...tempLand, items: [...tempLand.items, createEmptyLandItem()] }); };
  const removeLandItemField = (idx) => { const newItems = tempLand.items.filter((_, i) => i !== idx); setTempLand({ ...tempLand, items: newItems.length > 0 ? newItems : [createEmptyLandItem()] }); };
  const handleLandItemChange = (idx, field, value) => { const newItems = [...tempLand.items]; newItems[idx][field] = value; if (['areaM2', 'shareNum', 'shareDenom', 'pricePerPing'].includes(field)) { const area = Number(newItems[idx].areaM2)||0, num = Number(newItems[idx].shareNum)||0, denom = Number(newItems[idx].shareDenom)||1, price = Number(newItems[idx].pricePerPing)||0; const hM2 = area * (num / denom); newItems[idx].subtotal = Math.round(toPing(hM2) * price).toString(); } setTempLand({ ...tempLand, items: newItems }); };
  const saveLand = () => { if (tempLand.items.some(item => !item.lotNumber)) return alert("請填寫所有地號"); let totalM2=0, totalPingSum=0, totalPriceSum=0; tempLand.items.forEach(item => { const hM2 = Number(item.areaM2)*(Number(item.shareNum)/Number(item.shareDenom)); totalM2+=hM2; totalPingSum+=toPing(hM2); totalPriceSum+=(Number(item.subtotal)||0); }); const landData = { ...tempLand, holdingAreaM2: totalM2.toFixed(3), holdingAreaPing: totalPingSum.toFixed(3), totalPrice: totalPriceSum }; if (editingLandId) setLands(lands.map(l => l.id === editingLandId ? { ...landData, id: l.id } : l)); else setLands([...lands, { ...landData, id: Date.now() }]); setTempLand({ section: "", items: [createEmptyLandItem()], sellers: [] }); setShowLandForm(false); setEditingLandId(null); };
  const saveBuyer = () => { if (!newBuyer.name) return; if (editingBuyerId) setBuyers(buyers.map(b => b.id === editingBuyerId ? { ...b, ...newBuyer } : b)); else setBuyers([...buyers, { id: Date.now(), ...newBuyer }]); setEditingBuyerId(null); setNewBuyer({ name: "", phone: "", address: "", image: null }); };
  const addBuildingSeller = () => { if (!tempBuildingSeller.name) return; setTempBuilding({ ...tempBuilding, sellers: [...tempBuilding.sellers, { id: Date.now(), ...tempBuildingSeller }] }); setTempBuildingSeller({ name: "", phone: "", address: "" }); };
  const removeBuildingSeller = (id) => { setTempBuilding({ ...tempBuilding, sellers: tempBuilding.sellers.filter(s => s.id !== id) }); };
  const saveBuilding = () => { if(!tempBuilding.address) return alert("請輸入門牌地址"); if (editingBuildingId) setBuildings(buildings.map(b => b.id === editingBuildingId ? { ...tempBuilding, id: b.id } : b)); else setBuildings([...buildings, { ...tempBuilding, id: Date.now() }]); setTempBuilding({ permitNumber: "", address: "", license: "", buildNumber: "", areaM2: "", pricePerUnit: "", totalPrice: "", sellers: [], permitImage: null, licenseImage: null, buildNoImage: null }); setShowBuildingForm(false); setEditingBuildingId(null); };
  const saveTransaction = (e) => { if(e) e.preventDefault(); if (!newTx.amount) return; if (editingTxId) setTransactions(transactions.map(t => t.id === editingTxId ? { ...newTx, id: t.id, amount: Number(newTx.amount) } : t)); else setTransactions([...transactions, { ...newTx, id: Date.now(), amount: Number(newTx.amount) }]); setNewTx({ date: new Date().toISOString().split('T')[0], type: 'expense', category: CATEGORIES.expense[0], amount: '', note: '', image: null, linkedId: null, linkedType: 'general' }); setEditingTxId(null); };
  
  const handleExport = () => exportMasterCSV(projectName, buyers, lands, buildings, transactions, handoverData, projectTeam);
  const handlePrint = () => window.print();

  const allLotNumbers = lands.map(l => `${l.section} (${l.items.map(i=>i.lotNumber).join(',')})`).join('; ');
  const allBuildingInfo = buildings.map(b => `建號:${b.buildNumber} / 地址:${b.address}`).join('; ');

  // 交易表格元件 (Web View)
  const TransactionTable = ({ data, typeLabel, colorTheme, subStats }) => {
    const net = (subStats?.income || 0) - (subStats?.expense || 0);
    return (
        <div className={`rounded-2xl border bg-white overflow-hidden mb-8 shadow-md transition-all hover:shadow-lg ${colorTheme} break-inside-avoid`}>
        <div className="p-4 border-b bg-gray-50/50 flex justify-between items-center"><h4 className="font-black text-gray-700 flex items-center gap-2 text-lg">{typeLabel}</h4><span className="text-xs font-bold text-gray-400 bg-white px-2 py-1 rounded border">共 {data.length} 筆</span></div><div className="overflow-x-auto"><table className="w-full text-sm text-left"><thead className="text-xs text-gray-400 bg-gray-50 uppercase tracking-wider"><tr><th className="p-4 w-28 whitespace-nowrap">日期</th><th className="p-4 w-32 whitespace-nowrap">類型</th><th className="p-4 min-w-[200px]">項目 / 對象 / 備註</th><th className="p-4 text-right w-32 whitespace-nowrap">金額</th><th className="p-4 w-16 text-center print:hidden">操作</th></tr></thead><tbody className="divide-y divide-gray-100">{data.map(t => { let linkedLabel = "-"; if(t.linkedType === 'land') { const land = lands.find(l=>l.id===t.linkedId); linkedLabel = land ? (land.sellers.map(s=>s.name).join('/') || land.items[0]?.lotNumber) : '未知'; } else if(t.linkedType === 'building') { const build = buildings.find(b=>b.id===t.linkedId); linkedLabel = build ? (build.sellers.map(s=>s.name).join('/') || build.address.substring(0,8)) : '未知'; } return (<tr key={t.id} className="hover:bg-gray-50 group transition-colors"><td className="p-4 text-gray-500 font-mono text-sm whitespace-nowrap">{t.date}</td><td className="p-4 whitespace-nowrap"><span className={`px-3 py-1.5 rounded-full text-xs font-bold ${t.type==='income'?'bg-green-100 text-green-700':'bg-red-100 text-red-700'}`}>{t.category}</span></td><td className="p-4">{t.linkedType !== 'general' && <div className="text-xs font-bold text-gray-500 mb-1 bg-gray-100 inline-block px-2 py-0.5 rounded mr-2 whitespace-nowrap">{linkedLabel}</div>}<span className="text-gray-700 font-medium">{t.note || "-"}</span>{t.image && <button onClick={() => setPreviewImage(t.image)} className="text-xs text-blue-500 hover:text-blue-700 font-bold flex items-center gap-1 mt-1 whitespace-nowrap"><ImageIcon className="w-3 h-3"/> 查看憑證</button>}</td><td className={`p-4 text-right font-mono font-black text-base whitespace-nowrap ${t.type==='income'?'text-green-600':'text-red-600'}`}>${Number(t.amount).toLocaleString()}</td><td className="p-4 text-center print:hidden"><div className="flex justify-center gap-2 opacity-0 group-hover:opacity-100 transition"><button onClick={() => {setEditingTxId(t.id); setNewTx({...t});}} className="text-gray-400 hover:text-blue-600"><Edit2 className="w-4 h-4"/></button><button onClick={() => setTransactions(transactions.filter(item => item.id !== t.id))} className="text-gray-400 hover:text-red-500"><Trash2 className="w-4 h-4"/></button></div></td></tr>); })}{data.length === 0 && <tr><td colSpan="5" className="p-8 text-center text-gray-300 text-sm italic">尚無資料</td></tr>}</tbody><tfoot className="bg-gray-50 border-t-2 border-gray-100 text-xs"><tr><td colSpan="3" className="p-4 text-right font-bold text-gray-500 uppercase tracking-widest">本欄小計 (Subtotal)</td><td className="p-4 text-right whitespace-nowrap"><div className="flex justify-end gap-3 items-center"><div className="text-green-600 font-bold"><span className="text-[10px] text-gray-400 mr-1">收</span>${(subStats?.income || 0).toLocaleString()}</div><div className="text-red-500 font-bold"><span className="text-[10px] text-gray-400 mr-1">支</span>${(subStats?.expense || 0).toLocaleString()}</div></div><div className={`mt-1 pt-1 border-t border-gray-200 font-black text-sm ${net >= 0 ? 'text-blue-600' : 'text-orange-600'}`}><span className="text-[10px] text-gray-400 mr-1">淨</span>${net.toLocaleString()}</div></td><td className="print:hidden"></td></tr></tfoot></table></div></div>
    );
  };

  return (
    <div className="animate-fadeIn pb-24 text-base app-wrapper">
      <div className="print:hidden">
        {/* Top Bar (略) */}
        <div className="flex flex-col md:flex-row justify-between items-start md:items-center mb-8 gap-4 border-b pb-6">
          <div className="flex items-center gap-4"><button onClick={onBack} className="p-2 hover:bg-gray-200 rounded-full transition"><ArrowLeft className="w-6 h-6 text-gray-600" /></button><div><div className="text-sm text-gray-500 font-medium uppercase tracking-widest decoration-blue-500 underline underline-offset-4">專案管理工作區</div><div className="flex flex-wrap items-center gap-4 mt-1">{isEditingName ? (<div className="flex items-center gap-2"><input type="text" value={projectName} onChange={(e) => setProjectName(e.target.value)} className="text-3xl font-bold text-gray-800 border-b-2 border-blue-500 focus:outline-none bg-transparent" autoFocus /><button onClick={() => setIsEditingName(false)} className="text-green-600"><Save className="w-6 h-6" /></button></div>) : (<div className="flex items-center gap-2 group"><h1 className="text-3xl font-bold text-gray-800" title="點擊修改名稱">{projectName}</h1><button onClick={() => setIsEditingName(true)} className="text-gray-400 hover:text-blue-600 transition"><Edit2 className="w-5 h-5" /></button></div>)}<div className="flex items-center gap-2 bg-gray-100 p-1.5 rounded-lg border border-gray-200"><Building2 className="w-4 h-4 text-gray-500 ml-1" /><input className="bg-transparent text-sm font-bold text-gray-700 outline-none w-28 placeholder-gray-400" placeholder="輸入案場名稱" value={projectSite} onChange={(e) => setProjectSite(e.target.value)} /><div className="w-px h-4 bg-gray-300 mx-1"></div><Tag className="w-4 h-4 text-gray-500" /><select className="bg-transparent text-sm font-bold text-gray-600 outline-none cursor-pointer" value={projectZone} onChange={(e) => setProjectZone(e.target.value)}><option value="未分類">未分類</option><option value="A區">A區</option><option value="B區">B區</option><option value="C區">C區</option><option value="D區">D區</option><option value="E區">E區</option></select></div></div></div></div>
          <div className="flex gap-3"><div className="relative"><button onClick={() => setShowPrintSettings(!showPrintSettings)} className="flex items-center gap-2 px-4 py-2.5 bg-gray-100 text-gray-600 rounded-lg hover:bg-gray-200 text-sm shadow-sm transition font-bold"><Settings className="w-5 h-5" /> 匯出設定</button>{showPrintSettings && (<div className="absolute right-0 mt-2 w-64 bg-white rounded-xl shadow-2xl border border-gray-100 p-4 z-50 animate-fadeIn"><h4 className="text-xs font-black text-gray-400 uppercase tracking-widest mb-3">勾選要匯出的項目</h4><div className="space-y-2"><label className="flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer"><input type="checkbox" className="w-4 h-4 accent-blue-600" checked={printConfig.team} onChange={(e) => setPrintConfig({...printConfig, team: e.target.checked})} /><span className="text-sm font-bold text-gray-700">專案團隊</span></label><label className="flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer"><input type="checkbox" className="w-4 h-4 accent-blue-600" checked={printConfig.buyers} onChange={(e) => setPrintConfig({...printConfig, buyers: e.target.checked})} /><span className="text-sm font-bold text-gray-700">買受人</span></label><label className="flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer"><input type="checkbox" className="w-4 h-4 accent-blue-600" checked={printConfig.lands} onChange={(e) => setPrintConfig({...printConfig, lands: e.target.checked})} /><span className="text-sm font-bold text-gray-700">土地清單</span></label><label className="flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer"><input type="checkbox" className="w-4 h-4 accent-blue-600" checked={printConfig.buildings} onChange={(e) => setPrintConfig({...printConfig, buildings: e.target.checked})} /><span className="text-sm font-bold text-gray-700">建物清單</span></label><label className="flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer"><input type="checkbox" className="w-4 h-4 accent-blue-600" checked={printConfig.handover} onChange={(e) => setPrintConfig({...printConfig, handover: e.target.checked})} /><span className="text-sm font-bold text-gray-700">點交單</span></label><label className="flex items-center gap-3 p-2 hover:bg-gray-50 rounded-lg cursor-pointer"><input type="checkbox" className="w-4 h-4 accent-blue-600" checked={printConfig.finance} onChange={(e) => setPrintConfig({...printConfig, finance: e.target.checked})} /><span className="text-sm font-bold text-gray-700">財務報表</span></label></div></div>)}</div><button onClick={handleExport} className="flex items-center gap-2 px-4 py-2.5 bg-green-700 text-white rounded-lg hover:bg-green-800 text-sm shadow-sm transition font-bold"><FileSpreadsheet className="w-5 h-5" /> 匯出 CSV</button><button onClick={handlePrint} className="flex items-center gap-2 px-4 py-2.5 bg-gray-700 text-white rounded-lg hover:bg-gray-800 text-sm shadow-sm transition font-bold"><Printer className="w-5 h-5" /> 預覽/列印</button></div>
        </div>
        <div className="flex gap-3 mb-8 overflow-x-auto no-scrollbar"><button onClick={() => setActiveTab('team')} className={`flex items-center gap-2 px-6 py-3 rounded-xl font-bold whitespace-nowrap transition-all duration-200 text-sm ${activeTab === 'team' ? 'bg-blue-600 text-white shadow-md' : 'bg-white text-gray-600 hover:bg-gray-100 border'}`}><Briefcase className="w-5 h-5" /> 專案團隊</button><button onClick={() => setActiveTab('project')} className={`flex items-center gap-2 px-6 py-3 rounded-xl font-bold whitespace-nowrap transition-all duration-200 text-sm ${activeTab === 'project' ? 'bg-blue-600 text-white shadow-md' : 'bg-white text-gray-600 hover:bg-gray-100 border'}`}><Users className="w-5 h-5" /> 買受人資訊</button><button onClick={() => setActiveTab('land')} className={`flex items-center gap-2 px-6 py-3 rounded-xl font-bold whitespace-nowrap transition-all duration-200 text-sm ${activeTab === 'land' ? 'bg-blue-600 text-white shadow-md' : 'bg-white text-gray-600 hover:bg-gray-100 border'}`}><Map className="w-5 h-5" /> 土地標格</button><button onClick={() => setActiveTab('building')} className={`flex items-center gap-2 px-6 py-3 rounded-xl font-bold whitespace-nowrap transition-all duration-200 text-sm ${activeTab === 'building' ? 'bg-blue-600 text-white shadow-md' : 'bg-white text-gray-600 hover:bg-gray-100 border'}`}><Home className="w-5 h-5" /> 建物標格</button><button onClick={() => setActiveTab('handover')} className={`flex items-center gap-2 px-6 py-3 rounded-xl font-bold whitespace-nowrap transition-all duration-200 text-sm ${activeTab === 'handover' ? 'bg-blue-600 text-white shadow-md' : 'bg-white text-gray-600 hover:bg-gray-100 border'}`}><ClipboardCheck className="w-5 h-5" /> 交屋點交</button><button onClick={() => setActiveTab('finance')} className={`flex items-center gap-2 px-6 py-3 rounded-xl font-bold whitespace-nowrap transition-all duration-200 text-sm ${activeTab === 'finance' ? 'bg-blue-600 text-white shadow-md' : 'bg-white text-gray-600 hover:bg-gray-100 border'}`}><DollarSign className="w-5 h-5" /> 財務收支</button></div>

        {/* 0~4. 其他 Tab (略 - 保持原樣) */}
        {activeTab === 'team' && (<div className="bg-white rounded-2xl shadow-sm border p-8 animate-fadeIn"><h2 className="font-bold text-gray-700 mb-6 flex items-center gap-2 border-l-4 border-blue-500 pl-4 uppercase tracking-wider text-lg">專案團隊資訊 (Project Team)</h2><div className="grid grid-cols-1 md:grid-cols-2 gap-6 bg-gray-50 p-6 rounded-2xl border border-gray-100"><div><label className="text-sm text-gray-500 block mb-2 font-bold">仲介公司</label><select className="w-full p-3 border rounded-lg text-base bg-white" value={projectTeam.agency} onChange={e => handleAgencyChange(e.target.value)}><option value="">-- 請選擇 --</option>{agencyDB && Object.keys(agencyDB).map(ag => <option key={ag} value={ag}>{ag}</option>)}</select></div><div><label className="text-sm text-gray-500 block mb-2 font-bold">經紀人</label><select className="w-full p-3 border rounded-lg text-base bg-white" value={projectTeam.broker} onChange={e => setProjectTeam({...projectTeam, broker: e.target.value})} disabled={!projectTeam.agency}><option value="">{projectTeam.agency ? "-- 請選擇經紀人 --" : "-- 請先選擇公司 --"}</option>{projectTeam.agency && agencyDB[projectTeam.agency]?.map(bk => <option key={bk} value={bk}>{bk}</option>)}</select></div><div className="md:col-span-2 grid grid-cols-1 md:grid-cols-2 gap-4 border-t pt-4"><div><label className="text-sm text-gray-500 block mb-2 font-bold">開發業務 (人員)</label><input className="w-full p-3 border rounded-lg" value={projectTeam.developer} onChange={e=>setProjectTeam({...projectTeam, developer:e.target.value})} placeholder="開發人員姓名" /></div><div className="flex gap-2"><div className="flex-1"><label className="text-sm text-gray-500 block mb-2 font-bold">合約類型</label><select className="w-full p-3 border rounded-lg bg-white" value={projectTeam.developerType} onChange={e=>setProjectTeam({...projectTeam, developerType:e.target.value})}><option value="general">一般約</option><option value="exclusive">專任約</option></select></div><div className="flex-[2]"><label className="text-sm text-gray-500 block mb-2 font-bold">合約號碼</label><div className="flex gap-2"><input className="w-full p-3 border rounded-lg" value={projectTeam.developerNo} onChange={e=>setProjectTeam({...projectTeam, developerNo:e.target.value})} placeholder="合約編號" /><label className="cursor-pointer bg-gray-200 p-3 rounded-lg flex items-center justify-center text-gray-500 hover:bg-gray-300"><Camera className="w-5 h-5"/><input type="file" className="hidden" accept="image/*" onChange={(e)=>handleImageUploadGeneric(e.target.files[0], (res)=>setProjectTeam({...projectTeam, developerImg: res}))} /></label>{projectTeam.developerImg && <button onClick={()=>setPreviewImage(projectTeam.developerImg)} className="bg-blue-100 text-blue-600 p-3 rounded-lg font-bold text-xs whitespace-nowrap">查看</button>}</div></div></div></div><div className="md:col-span-2 grid grid-cols-1 md:grid-cols-2 gap-4 border-t pt-4"><div><label className="text-sm text-gray-500 block mb-2 font-bold">行銷業務 (人員)</label><input className="w-full p-3 border rounded-lg" value={projectTeam.marketer} onChange={e=>setProjectTeam({...projectTeam, marketer:e.target.value})} placeholder="行銷人員姓名" /></div><div className="flex gap-2"><div className="flex-1 pt-8 text-sm font-bold text-gray-400 text-center bg-gray-100 rounded-lg">斡旋 / 訂金</div><div className="flex-[2]"><label className="text-sm text-gray-500 block mb-2 font-bold">單據號碼</label><div className="flex gap-2"><input className="w-full p-3 border rounded-lg" value={projectTeam.marketerNo} onChange={e=>setProjectTeam({...projectTeam, marketerNo:e.target.value})} placeholder="單據編號" /><label className="cursor-pointer bg-gray-200 p-3 rounded-lg flex items-center justify-center text-gray-500 hover:bg-gray-300"><Camera className="w-5 h-5"/><input type="file" className="hidden" accept="image/*" onChange={(e)=>handleImageUploadGeneric(e.target.files[0], (res)=>setProjectTeam({...projectTeam, marketerImg: res}))} /></label>{projectTeam.marketerImg && <button onClick={()=>setPreviewImage(projectTeam.marketerImg)} className="bg-blue-100 text-blue-600 p-3 rounded-lg font-bold text-xs whitespace-nowrap">查看</button>}</div></div></div></div><div className="md:col-span-2 border-t pt-4"><label className="text-sm text-gray-500 block mb-2 font-bold">承辦代書</label><input className="w-full p-3 border rounded-lg text-base" value={projectTeam.scrivener} onChange={e=>setProjectTeam({...projectTeam, scrivener:e.target.value})} placeholder="代書姓名/事務所" /></div></div></div>)}
        {activeTab === 'project' && (<div className="bg-white rounded-2xl shadow-sm border p-8 animate-fadeIn"><h2 className="font-bold text-gray-700 mb-6 flex items-center gap-2 border-l-4 border-blue-500 pl-4 uppercase tracking-wider text-lg">買受人資訊管理</h2><div className="grid grid-cols-1 md:grid-cols-5 gap-4 items-end mb-8 bg-gray-50 p-6 rounded-xl border border-gray-100"><input type="text" placeholder="姓名" className="w-full p-3 border rounded-lg text-base" value={newBuyer.name} onChange={e => setNewBuyer({...newBuyer, name: e.target.value})} /><input type="text" placeholder="電話" className="w-full p-3 border rounded-lg text-base" value={newBuyer.phone} onChange={e => setNewBuyer({...newBuyer, phone: e.target.value})} /><input type="text" placeholder="地址" className="md:col-span-2 w-full p-3 border rounded-lg text-base" value={newBuyer.address} onChange={e => setNewBuyer({...newBuyer, address: e.target.value})} /><div className="relative"><input type="file" id="buyerImg" className="hidden" accept="image/*" onChange={(e) => handleImageUploadGeneric(e.target.files[0], (res) => setNewBuyer({...newBuyer, image: res}))} /><label htmlFor="buyerImg" className={`flex justify-center items-center gap-2 w-full p-3 border-2 border-dashed rounded-lg text-xs font-bold cursor-pointer transition-all ${newBuyer.image ? 'bg-blue-50 border-blue-400 text-blue-600' : 'bg-white border-gray-300'}`}>{newBuyer.image ? <Check className="w-4 h-4"/> : <Camera className="w-4 h-4"/>} {newBuyer.image ? "已選圖" : "插入證件圖"}</label>{newBuyer.image && <button onClick={()=>setNewBuyer({...newBuyer, image: null})} className="absolute -top-2 -right-2 bg-red-500 text-white rounded-full p-1 shadow-md hover:bg-red-600 transition"><X className="w-3 h-3"/></button>}</div><button onClick={saveBuyer} className="md:col-span-5 w-full py-3 rounded-lg text-white text-base font-bold bg-blue-600 hover:bg-blue-700 shadow-md">{editingBuyerId ? "更新" : "新增買受人"}</button></div><div className="space-y-3">{buyers.map(b => (<div key={b.id} className="flex justify-between items-center p-4 border rounded-xl hover:bg-gray-50 transition group"><div className="flex-1 grid grid-cols-1 md:grid-cols-4 gap-4 text-base items-center"><div className="font-bold text-gray-800 underline decoration-blue-200">{b.name}</div><div className="text-gray-600">{b.phone}</div><div className="text-gray-500 truncate">{b.address}</div><div>{b.image && <button onClick={() => setPreviewImage(b.image)} className="text-xs bg-gray-100 px-2 py-1 rounded flex items-center gap-1 hover:bg-gray-200"><ImageIcon className="w-3 h-3"/> 查看證件</button>}</div></div><div className="flex gap-2 ml-4"><button onClick={() => {setEditingBuyerId(b.id); setNewBuyer({...b});}} className="text-gray-400 hover:text-blue-600 p-2"><Edit2 className="w-5 h-5" /></button><button onClick={() => setBuyers(buyers.filter(item => item.id !== b.id))} className="text-gray-400 hover:text-red-500 p-2"><Trash2 className="w-5 h-5" /></button></div></div>))}</div></div>)}
        {activeTab === 'land' && (<div className="space-y-6 animate-fadeIn"><div className="bg-gradient-to-r from-blue-600 to-blue-800 p-6 rounded-3xl text-white shadow-xl mb-6"><h3 className="text-lg font-black mb-4 flex items-center gap-2"><Map className="w-6 h-6"/> 全案土地總結算</h3><div className="grid grid-cols-3 gap-6 text-center"><div className="bg-white/10 p-4 rounded-xl backdrop-blur-sm"><span className="block text-xs text-blue-200 font-bold mb-1">總持有面積 (㎡)</span><span className="text-3xl font-black">{landGrandTotal.m2}</span></div><div className="bg-white/10 p-4 rounded-xl backdrop-blur-sm"><span className="block text-xs text-blue-200 font-bold mb-1">總持有坪數</span><span className="text-3xl font-black">{landGrandTotal.ping}</span></div><div className="bg-white/10 p-4 rounded-xl backdrop-blur-sm"><span className="block text-xs text-blue-200 font-bold mb-1">總金額 ($)</span><span className="text-3xl font-black">${landGrandTotal.price.toLocaleString()}</span></div></div></div>{!showLandForm && <button onClick={() => { setEditingLandId(null); setTempLand({ section: "", items: [createEmptyLandItem()], sellers: [] }); setShowLandForm(true); }} className="w-full py-6 border-2 border-dashed rounded-2xl text-gray-400 hover:border-blue-500 hover:text-blue-500 flex justify-center items-center gap-2 transition bg-white shadow-sm text-lg font-bold"><Plus className="w-6 h-6" /> 錄入土地標的資訊 (多筆錄入)</button>}{showLandForm && (<div className="bg-white p-8 rounded-3xl shadow-xl border border-blue-100 animate-fadeIn"><div className="flex justify-between mb-8 font-bold text-blue-900 border-b pb-4"><h3 className="flex items-center gap-2 text-xl"><Map className="w-7 h-7" /> {editingLandId ? "修改標的" : "新增土地標的"}</h3><button onClick={() => setShowLandForm(false)} className="text-gray-400 hover:bg-gray-100 p-2 rounded-full transition"><X className="w-7 h-7" /></button></div><div className="bg-gray-50 p-6 rounded-2xl mb-8 border border-gray-100"><h4 className="text-xs font-black text-gray-400 mb-4 uppercase tracking-[0.2em]">步驟 1: 土地出售人</h4><div className="flex flex-col md:flex-row gap-4 mb-4"><div className="flex-1"><input list="pre-sellers" placeholder="出售人姓名" className="w-full p-3 border rounded-lg text-base outline-none bg-white" value={tempLandSeller.name} onChange={e => setTempLandSeller({...tempLandSeller, name: e.target.value})} /><datalist id="pre-sellers">{PREDEFINED_SELLERS.map(n => <option key={n} value={n} />)}</datalist></div><input placeholder="電話" className="flex-1 p-3 border rounded-lg text-base outline-none bg-white" value={tempLandSeller.phone} onChange={e => setTempLandSeller({...tempLandSeller, phone: e.target.value})} /><button onClick={addLandSeller} className="bg-gray-800 text-white px-8 rounded-lg font-black hover:bg-black transition shadow-lg text-sm">加入</button></div><div className="space-y-2">{tempLand.sellers.map(s => <div key={s.id} className="text-sm flex justify-between items-center p-3 bg-white border rounded-lg shadow-sm"><span>{s.name} | {s.phone}</span> <button onClick={() => removeLandSeller(s.id)} className="text-red-400 hover:bg-red-50 p-1 rounded-full"><Trash2 className="w-4 h-4" /></button></div>)}</div></div><div className="mb-8"><div className="flex justify-between items-center mb-4"><h4 className="text-xs font-black text-gray-400 uppercase tracking-[0.2em]">步驟 2: 地號規格</h4><input placeholder="地段 (如：仁武段)" className="p-2 border rounded text-sm outline-none focus:ring-2 focus:ring-blue-100 w-40" value={tempLand.section} onChange={e => setTempLand({...tempLand, section: e.target.value})} /></div><div className="overflow-x-auto"><table className="w-full text-sm text-left border-collapse bg-white"><thead className="text-xs font-black uppercase text-gray-400 tracking-wider"><tr className="border-b"><th className="p-3 w-24">地號</th><th className="p-3 w-24">面積(㎡)</th><th className="p-3 w-16">分子</th><th className="p-3 w-16">分母</th><th className="p-3 w-28">單價</th><th className="p-3 text-right">小計金額</th><th className="p-3 w-12"></th></tr></thead><tbody className="divide-y">{tempLand.items.map((item, idx) => (<tr key={item.id} className="hover:bg-blue-50/30 transition"><td className="p-2"><input className="w-full p-2 border rounded outline-none focus:bg-white bg-transparent" value={item.lotNumber} onChange={e => handleLandItemChange(idx, 'lotNumber', e.target.value)} /></td><td className="p-2"><input type="number" className="w-full p-2 border rounded outline-none focus:bg-white bg-transparent" value={item.areaM2} onChange={e => handleLandItemChange(idx, 'areaM2', e.target.value)} /></td><td className="p-2"><input className="w-full p-2 border rounded outline-none text-center bg-transparent" value={item.shareNum} onChange={e => handleLandItemChange(idx, 'shareNum', e.target.value)} /></td><td className="p-2"><input className="w-full p-2 border rounded outline-none text-center bg-transparent" value={item.shareDenom} onChange={e => handleLandItemChange(idx, 'shareDenom', e.target.value)} /></td><td className="p-2"><input type="number" className="w-full p-2 border rounded outline-none bg-transparent" value={item.pricePerPing} onChange={e => handleLandItemChange(idx, 'pricePerPing', e.target.value)} /></td><td className="p-2 text-right relative"><input type="number" className="w-full p-2 pl-6 border border-blue-100 rounded outline-none font-mono font-black text-blue-600 text-right bg-blue-50/20 focus:bg-white" value={item.subtotal} onChange={e => handleLandItemChange(idx, 'subtotal', e.target.value)} /></td><td className="p-2"><button onClick={() => removeLandItemField(idx)} className="text-red-400 p-2 hover:bg-red-50 rounded"><Minus className="w-4 h-4" /></button></td></tr>))}</tbody></table><button onClick={addLandItemField} className="mt-4 w-full py-3 border-2 border-dashed rounded-xl text-blue-500 hover:bg-blue-50 transition flex justify-center items-center gap-1 font-bold text-sm"><Plus className="w-5 h-5" /> 增加地號行</button></div></div><button onClick={saveLand} className="w-full py-5 rounded-2xl text-white font-black bg-blue-600 shadow-2xl transition-all hover:bg-blue-700 hover:scale-[1.01] active:scale-100 tracking-[0.3em] uppercase font-bold text-lg">儲存土地標的</button></div>)}<div className="grid grid-cols-1 gap-6">{lands.map(l => (<div key={l.id} className="bg-white p-8 rounded-[2rem] shadow-sm border border-gray-100 hover:shadow-xl hover:border-blue-100 transition-all duration-500 group"><div className="flex justify-between items-start mb-6"><div className="flex-1"><div className="flex items-center gap-3 mb-4"><span className="bg-blue-600 text-white text-xs px-3 py-1 rounded-full font-black tracking-widest">土地標的</span><h4 className="font-black text-gray-900 text-2xl">{l.sellers.length > 0 ? l.sellers.map(s => s.name).join(' / ') : `地段: ${l.section || "未命名"}`}</h4></div><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 text-base text-gray-500 bg-gray-50 p-6 rounded-2xl border border-gray-100 shadow-inner"><div><span className="text-xs text-gray-400 block font-black uppercase mb-1">地號總數</span><p className="font-black text-gray-700">{l.items.length} 筆</p></div><div><span className="text-xs text-gray-400 block font-black uppercase mb-1">持有 (㎡)</span><p className="font-mono font-bold text-gray-700">{Number(l.holdingAreaM2).toFixed(3)}</p></div><div><span className="text-xs text-gray-400 block font-black uppercase mb-1">持有 (坪)</span><p className="font-mono font-bold text-gray-700">{Number(l.holdingAreaPing).toFixed(3)}</p></div><div><span className="text-xs text-blue-500 block font-black uppercase mb-1">成交總額</span><p className="font-mono font-black text-blue-600 text-xl">${Number(l.totalPrice).toLocaleString()}</p></div></div></div><div className="flex gap-2 ml-4"><button onClick={() => { setEditingLandId(l.id); setTempLand({...l}); setShowLandForm(true); }} className="p-3 text-gray-300 hover:text-blue-600 transition hover:bg-blue-50 rounded-full"><Edit2 className="w-5 h-5" /></button><button onClick={() => setLands(lands.filter(item => item.id !== l.id))} className="p-3 text-gray-300 hover:text-red-500 transition hover:bg-red-50 rounded-full"><Trash2 className="w-5 h-5" /></button></div></div><LinkedLedger linkedId={l.id} linkedType="land" transactions={transactions} onSaveTransaction={(tx) => setTransactions([...transactions, tx])} /></div>))}</div></div>)}
        {activeTab === 'building' && (<div className="space-y-6 animate-fadeIn">{!showBuildingForm && <button onClick={() => { setEditingBuildingId(null); setTempBuilding({ permitNumber: "", address: "", license: "", buildNumber: "", areaM2: "", pricePerUnit: "", totalPrice: "", sellers: [], permitImage: null, licenseImage: null, buildNoImage: null }); setShowBuildingForm(true); }} className="w-full py-6 border-2 border-dashed rounded-2xl text-gray-400 hover:border-orange-500 hover:text-orange-500 flex justify-center items-center gap-2 transition bg-white shadow-sm text-lg font-bold"><Plus className="w-6 h-6" /> 新增建物案場資料</button>}{showBuildingForm && (<div className="bg-white p-8 rounded-3xl shadow-xl border border-orange-200 animate-fadeIn"><div className="flex justify-between mb-8 font-bold text-orange-900 border-b pb-4"><h3 className="flex items-center gap-2 text-xl"><Home className="w-7 h-7" /> {editingBuildingId ? "修改建物資訊" : "新增建物案場"}</h3><button onClick={() => setShowBuildingForm(false)} className="text-gray-400 hover:bg-gray-100 p-2 rounded-full transition"><X className="w-7 h-7" /></button></div><div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8"><div className="md:col-span-2"><label className="text-sm text-gray-500 block mb-2 font-bold">建照號碼</label><div className="flex gap-2"><input placeholder="建照號碼" className="flex-1 w-full p-3 border rounded-lg text-base" value={tempBuilding.permitNumber} onChange={e => setTempBuilding({...tempBuilding, permitNumber: e.target.value})} /><label className="cursor-pointer bg-gray-100 hover:bg-gray-200 p-3 rounded-lg flex items-center gap-2 text-xs font-bold text-gray-500"><Camera className="w-4 h-4"/>{tempBuilding.permitImage ? "已存" : "圖檔"}<input type="file" className="hidden" accept="image/*" onChange={(e)=>handleImageUploadGeneric(e.target.files[0], (res)=>setTempBuilding({...tempBuilding, permitImage: res}))} /></label>{tempBuilding.permitImage && <button onClick={()=>setTempBuilding({...tempBuilding, permitImage: null})} className="bg-red-50 text-red-500 p-3 rounded-lg"><X className="w-4 h-4"/></button>}</div></div><div className="md:col-span-2"><label className="text-sm text-gray-500 block mb-2 font-bold">門牌地址</label><input placeholder="完整門牌地址" className="w-full p-3 border rounded-lg text-base" value={tempBuilding.address} onChange={e => setTempBuilding({...tempBuilding, address: e.target.value})} /></div><div><label className="text-sm text-gray-500 block mb-2 font-bold">使用執照號碼</label><div className="flex gap-2"><input placeholder="使照號碼" className="flex-1 w-full p-3 border rounded-lg text-base" value={tempBuilding.license} onChange={e => setTempBuilding({...tempBuilding, license: e.target.value})} /><label className="cursor-pointer bg-gray-100 hover:bg-gray-200 p-3 rounded-lg flex items-center gap-2 text-xs font-bold text-gray-500"><Camera className="w-4 h-4"/>{tempBuilding.licenseImage ? "已存" : "圖檔"}<input type="file" className="hidden" accept="image/*" onChange={(e)=>handleImageUploadGeneric(e.target.files[0], (res)=>setTempBuilding({...tempBuilding, licenseImage: res}))} /></label>{tempBuilding.licenseImage && <button onClick={()=>setTempBuilding({...tempBuilding, licenseImage: null})} className="bg-red-50 text-red-500 p-3 rounded-lg"><X className="w-4 h-4"/></button>}</div></div><div><label className="text-sm text-gray-500 block mb-2 font-bold">建物建號</label><div className="flex gap-2"><input placeholder="建號" className="flex-1 w-full p-3 border rounded-lg text-base" value={tempBuilding.buildNumber} onChange={e => setTempBuilding({...tempBuilding, buildNumber: e.target.value})} /><label className="cursor-pointer bg-gray-100 hover:bg-gray-200 p-3 rounded-lg flex items-center gap-2 text-xs font-bold text-gray-500"><Camera className="w-4 h-4"/>{tempBuilding.buildNoImage ? "已存" : "圖檔"}<input type="file" className="hidden" accept="image/*" onChange={(e)=>handleImageUploadGeneric(e.target.files[0], (res)=>setTempBuilding({...tempBuilding, buildNoImage: res}))} /></label>{tempBuilding.buildNoImage && <button onClick={()=>setTempBuilding({...tempBuilding, buildNoImage: null})} className="bg-red-50 text-red-500 p-3 rounded-lg"><X className="w-4 h-4"/></button>}</div></div><div><label className="text-sm text-gray-500 block mb-2 font-bold">建物面積(㎡)</label><input type="number" className="w-full p-3 border rounded-lg text-base" value={tempBuilding.areaM2} onChange={e => setTempBuilding({...tempBuilding, areaM2: e.target.value})} /></div><div><label className="text-sm text-gray-500 block mb-2 text-orange-600 font-bold underline font-black">單價 (元/坪)</label><input type="number" className="w-full p-3 border border-orange-200 rounded-lg text-base bg-orange-50/20" value={tempBuilding.pricePerUnit} onChange={e => setTempBuilding({...tempBuilding, pricePerUnit: e.target.value})} /></div><div className="md:col-span-2"><label className="text-sm text-gray-500 block mb-2 text-orange-600 font-bold underline font-black">成交總金額 (元)</label><input type="number" className="w-full p-3 border border-orange-200 rounded-lg text-base bg-orange-50/20 font-bold" value={tempBuilding.totalPrice} onChange={e => setTempBuilding({...tempBuilding, totalPrice: e.target.value})} /></div></div><div className="bg-orange-50/30 p-6 rounded-2xl mb-8 border border-orange-100"><h4 className="text-xs font-bold text-orange-700 mb-4 uppercase tracking-wider">屋主/出售人資訊</h4><div className="flex flex-col md:flex-row gap-4 mb-4"><div className="flex-1"><input list="pre-sellers" placeholder="姓名" className="w-full p-3 border rounded-lg text-base outline-none bg-white" value={tempBuildingSeller.name} onChange={e => setTempBuildingSeller({...tempBuildingSeller, name: e.target.value})} /></div><input placeholder="電話" className="flex-1 p-3 border rounded-lg text-base outline-none bg-white" value={tempBuildingSeller.phone} onChange={e => setTempBuildingSeller({...tempBuildingSeller, phone: e.target.value})} /><button onClick={addBuildingSeller} className="bg-orange-600 text-white px-8 rounded-lg text-sm hover:bg-orange-700 transition shadow-sm font-bold">加入</button></div><div className="space-y-2">{tempBuilding.sellers.map(s => <div key={s.id} className="text-sm flex justify-between items-center p-3 bg-white border rounded-lg shadow-sm"><span>{s.name} | {s.phone}</span> <button onClick={() => removeBuildingSeller(s.id)} className="text-red-400 hover:bg-red-50 p-1 rounded-full"><Trash2 className="w-4 h-4" /></button></div>)}</div></div><button onClick={saveBuilding} className="w-full py-5 rounded-2xl text-white font-black bg-orange-600 shadow-xl transition-all hover:bg-orange-700 hover:scale-[1.01] tracking-widest uppercase font-bold text-lg">儲存建物標的</button></div>)}<div className="grid grid-cols-1 gap-6">{buildings.map(b => (<div key={b.id} className="bg-white p-8 rounded-[2rem] shadow-sm border border-gray-100 hover:shadow-xl hover:border-orange-100 transition-all duration-500 group"><div className="flex justify-between items-start"><div className="flex-1"><div className="flex items-center gap-2 mb-3"><span className="bg-orange-100 text-orange-700 text-xs px-3 py-1 rounded-full font-bold tracking-tighter">建物案場</span><h4 className="font-black text-gray-900 text-2xl">{b.sellers.length > 0 ? b.sellers.map(s => s.name).join(' / ') : `地址: ${b.address}`}</h4></div><div className="grid grid-cols-1 md:grid-cols-3 gap-6 text-base text-gray-500 bg-gray-50 p-6 rounded-2xl border border-gray-100 shadow-inner"><div><span className="text-xs text-gray-400 block font-black uppercase mb-1">出售人/屋主</span>{b.sellers.length > 0 ? b.sellers.map(s => s.name).join(' / ') : "-"}</div><div><span className="text-xs text-gray-400 block font-black uppercase mb-1">建照號碼</span><div className="flex items-center gap-2">{b.permitNumber || "-"} {b.permitImage && <ImageIcon className="w-4 h-4 text-blue-500 cursor-pointer" onClick={()=>setPreviewImage(b.permitImage)}/>}</div></div><div><span className="text-xs text-gray-400 block font-black uppercase mb-1">地址</span>{b.address}</div><div><span className="text-xs text-gray-400 block font-black uppercase mb-1">建號</span><div className="flex items-center gap-2">{b.buildNumber} {b.buildNoImage && <ImageIcon className="w-4 h-4 text-blue-500 cursor-pointer" onClick={()=>setPreviewImage(b.buildNoImage)}/>}</div></div><div className="md:col-span-2"><span className="text-xs text-orange-500 block font-black uppercase mb-1">總額</span><span className="font-mono font-black text-orange-600 text-xl">${Number(b.totalPrice).toLocaleString()}</span></div></div></div><div className="flex gap-3 ml-4"><button onClick={() => { setEditingBuildingId(b.id); setTempBuilding({...b}); setShowBuildingForm(true); }} className="p-3 text-gray-300 hover:text-orange-600 transition hover:bg-orange-50 rounded-full"><Edit2 className="w-5 h-5" /></button><button onClick={() => setBuildings(buildings.filter(item => item.id !== b.id))} className="p-3 text-gray-300 hover:text-red-500 transition hover:bg-red-50 rounded-full"><Trash2 className="w-5 h-5" /></button></div></div><LinkedLedger linkedId={b.id} linkedType="building" transactions={transactions} onSaveTransaction={(tx) => setTransactions([...transactions, tx])} /></div>))}</div></div>)}
        {activeTab === 'handover' && (<div className="bg-white rounded-2xl shadow-sm border p-8 animate-fadeIn"><div className="border-b pb-6 mb-6"><h2 className="text-xl font-black text-gray-800 mb-4 flex items-center gap-2"><ClipboardCheck className="w-6 h-6 text-green-600"/> 交屋點交確認單</h2><div className="flex items-center gap-4 bg-yellow-50 p-4 rounded-xl border border-yellow-100 mb-6"><label className="font-bold text-yellow-800 flex items-center gap-2"><CalendarIcon className="w-5 h-5"/> 點交日期</label><input type="date" className="p-2 border rounded-lg bg-white outline-none focus:ring-2 focus:ring-yellow-400" value={handoverData.handoverDate || ""} onChange={(e)=>setHandoverData({...handoverData, handoverDate: e.target.value})} /></div><div className="bg-gray-50 p-4 rounded-xl text-sm text-gray-600 space-y-2 font-mono"><p><span className="font-bold text-gray-400">地籍地號：</span> {allLotNumbers || "無資料"}</p><p><span className="font-bold text-gray-400">建物資訊：</span> {allBuildingInfo || "無資料"}</p></div></div><div className="grid grid-cols-1 md:grid-cols-2 gap-8"><div className="space-y-6"><div><label className="font-bold text-gray-700 block mb-2">捲門遙控器數量 (0-4)</label><select className="w-full p-3 border rounded-lg bg-white" value={handoverData.remotes} onChange={(e)=>setHandoverData({...handoverData, remotes: e.target.value})}>{[0,1,2,3,4].map(n=><option key={n} value={n}>{n} 顆</option>)}</select></div><div className="flex gap-4"><div className="flex-1"><label className="font-bold text-gray-700 block mb-2"><Key className="w-4 h-4 inline mr-1"/> 小門鑰匙 (前)</label><select className="w-full p-3 border rounded-lg bg-white" value={handoverData.keysFront} onChange={(e)=>setHandoverData({...handoverData, keysFront: e.target.value})}>{[0,1,2,3,4,5,6].map(n=><option key={n} value={n}>{n} 支</option>)}</select></div><div className="flex-1"><label className="font-bold text-gray-700 block mb-2"><Key className="w-4 h-4 inline mr-1"/> 小門鑰匙 (後)</label><select className="w-full p-3 border rounded-lg bg-white" value={handoverData.keysBack} onChange={(e)=>setHandoverData({...handoverData, keysBack: e.target.value})}>{[0,1,2,3,4,5,6].map(n=><option key={n} value={n}>{n} 支</option>)}</select></div></div></div><div className="space-y-4"><label className="flex items-center gap-3 p-3 border rounded-lg cursor-pointer hover:bg-gray-50"><input type="checkbox" className="w-5 h-5 accent-green-600" checked={handoverData.warranty} onChange={(e)=>setHandoverData({...handoverData, warranty: e.target.checked})} /><span className="font-bold text-gray-700">廠房保固書 (一份)</span></label><label className="flex items-center gap-3 p-3 border rounded-lg cursor-pointer hover:bg-gray-50"><input type="checkbox" className="w-5 h-5 accent-green-600" checked={handoverData.drawings} onChange={(e)=>setHandoverData({...handoverData, drawings: e.target.checked})} /><span className="font-bold text-gray-700">廠房竣工圖 (一份)</span></label><label className="flex items-center gap-3 p-3 border rounded-lg cursor-pointer hover:bg-gray-50"><input type="checkbox" className="w-5 h-5 accent-green-600" checked={handoverData.originalPermit} onChange={(e)=>setHandoverData({...handoverData, originalPermit: e.target.checked})} /><span className="font-bold text-gray-700">使用執照正本 (一份)</span></label><div className="grid grid-cols-2 gap-4 mt-4"><div><label className="font-bold text-gray-700 block mb-1">電單號碼</label><div className="flex items-center gap-2"><span className="text-xl font-black text-gray-300">【</span><input type="text" className="w-full text-center border-b-2 border-gray-300 focus:border-green-500 outline-none text-xl font-mono" value={handoverData.electricityBill} onChange={(e)=>setHandoverData({...handoverData, electricityBill: e.target.value})} /><span className="text-xl font-black text-gray-300">】</span></div></div><div><label className="font-bold text-gray-700 block mb-1">水單號碼</label><div className="flex items-center gap-2"><span className="text-xl font-black text-gray-300">【</span><input type="text" className="w-full text-center border-b-2 border-gray-300 focus:border-blue-500 outline-none text-xl font-mono" value={handoverData.waterBill} onChange={(e)=>setHandoverData({...handoverData, waterBill: e.target.value})} /><span className="text-xl font-black text-gray-300">】</span></div></div></div></div></div></div>)}

        {/* 5. 財務 Tab (Web View) */}
        {activeTab === 'finance' && (
           <div className="space-y-8 animate-fadeIn">
              {/* 頂部總計 */}
              <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                 <div className="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 flex flex-col justify-center h-32"><div className="text-sm text-gray-400 font-black uppercase mb-2 tracking-widest text-center">累計總收入</div><div className="text-4xl font-black text-green-600 text-center">${stats.totalIncome.toLocaleString()}</div></div>
                 <div className="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 flex flex-col justify-center h-32"><div className="text-sm text-gray-400 font-black uppercase mb-2 tracking-widest text-center">累計總支出</div><div className="text-4xl font-black text-red-500 text-center">${stats.totalExpense.toLocaleString()}</div></div>
                 <div className={`p-6 rounded-2xl shadow-xl flex flex-col justify-center h-32 ${stats.netProfit >= 0 ? 'bg-blue-600 text-white' : 'bg-orange-600 text-white'}`}><div className="text-sm text-white/60 font-black uppercase mb-2 tracking-widest text-center">當前總損益 (ROI: {stats.roi}%)</div><div className="text-4xl font-black text-center">${stats.netProfit.toLocaleString()}</div></div>
              </div>
              
              {/* 分項小計卡片 */}
              <div className="bg-white p-8 rounded-3xl shadow-sm border border-gray-200">
                <h3 className="text-xs font-black text-gray-400 mb-6 flex items-center gap-2 uppercase tracking-[0.2em]"><Calculator className="w-5 h-5 text-blue-500" /> 分項財務小計 (Subtotals)</h3>
                <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                  {['general', 'land', 'building'].map(key => {
                     const bg = key==='general' ? 'bg-gray-50 border-gray-200 text-gray-700' : key==='land' ? 'bg-blue-50 border-blue-100 text-blue-800' : 'bg-orange-50 border-orange-100 text-orange-800';
                     const label = key==='general' ? '一般專案收支' : key==='land' ? '土地出售人帳目' : '建物出售人帳目';
                     const Icon = key==='general' ? DollarSign : key==='land' ? Map : Home;
                     const profit = (stats.subTotals[key]?.income||0) - (stats.subTotals[key]?.expense||0);
                     return (
                        <div key={key} className={`p-6 rounded-2xl border-2 ${bg}`}>
                           <div className="font-black mb-4 flex items-center gap-2 text-sm uppercase tracking-widest"><Icon className="w-5 h-5"/> {label}</div>
                           <div className="space-y-3 text-sm">
                              <div className="flex justify-between"><span>收入：</span><span className="font-mono font-bold text-green-600">${(stats.subTotals[key]?.income || 0).toLocaleString()}</span></div>
                              <div className="flex justify-between"><span>支出：</span><span className="font-mono font-bold text-red-500">${(stats.subTotals[key]?.expense || 0).toLocaleString()}</span></div>
                              <div className="flex justify-between border-t border-black/10 pt-3 mt-3 font-black"><span>淨利：</span><span className={`font-mono ${profit >= 0 ? 'text-blue-600' : 'text-orange-600'}`}>${profit.toLocaleString()}</span></div>
                           </div>
                        </div>
                     )
                  })}
                </div>
              </div>

              {/* 勾選顯示控制 */}
              <div className="bg-gray-50 border border-gray-200 rounded-xl p-4 flex flex-wrap gap-6 items-center">
                 <span className="text-xs font-black text-gray-400 uppercase tracking-widest flex items-center gap-1"><Filter className="w-4 h-4"/> 顯示篩選:</span>
                 <label className="flex items-center gap-2 cursor-pointer font-bold text-gray-700 select-none hover:text-black"><input type="checkbox" className="w-5 h-5 accent-gray-700 rounded" checked={visibleLedgers.general} onChange={(e)=>setVisibleLedgers({...visibleLedgers, general: e.target.checked})} /><span>一般專案收支</span></label>
                 <label className="flex items-center gap-2 cursor-pointer font-bold text-blue-700 select-none hover:text-blue-900"><input type="checkbox" className="w-5 h-5 accent-blue-600 rounded" checked={visibleLedgers.land} onChange={(e)=>setVisibleLedgers({...visibleLedgers, land: e.target.checked})} /><span>土地出售人帳目</span></label>
                 <label className="flex items-center gap-2 cursor-pointer font-bold text-orange-700 select-none hover:text-orange-900"><input type="checkbox" className="w-5 h-5 accent-orange-500 rounded" checked={visibleLedgers.building} onChange={(e)=>setVisibleLedgers({...visibleLedgers, building: e.target.checked})} /><span>建物出售人帳目</span></label>
              </div>

              {/* 交易列表 */}
              <div className="space-y-8">
                 {visibleLedgers.general && <TransactionTable data={groupedTransactions.general} typeLabel="一般專案收支明細" colorTheme="border-gray-200 shadow-sm" subStats={stats.subTotals.general} />}
                 {visibleLedgers.land && <TransactionTable data={groupedTransactions.land} typeLabel="土地出售人帳目明細" colorTheme="border-blue-200 shadow-sm" subStats={stats.subTotals.land} />}
                 {visibleLedgers.building && <TransactionTable data={groupedTransactions.building} typeLabel="建物出售人帳目明細" colorTheme="border-orange-200 shadow-sm" subStats={stats.subTotals.building} />}
                 {!visibleLedgers.general && !visibleLedgers.land && !visibleLedgers.building && <div className="p-12 text-center text-gray-400 border-2 border-dashed rounded-xl italic">已隱藏所有帳目，請勾選上方類別以檢視。</div>}
              </div>

              {/* 新增/修改表單 */}
              <div className={`p-8 rounded-3xl shadow-xl border transition-all ${editingTxId ? 'bg-orange-50 border-orange-200' : 'bg-white border-gray-100'}`}>
                 <h3 className="font-black text-gray-800 mb-8 flex justify-between items-center text-base uppercase tracking-widest border-b pb-4"><span className="flex items-center gap-2">{editingTxId ? <><Edit2 className="w-6 h-6 text-orange-600" /> 修改收支資料</> : <><Plus className="w-6 h-6 text-blue-600" /> 登錄專案收支</>}</span>{editingTxId && <button onClick={() => setEditingTxId(null)} className="text-sm text-gray-400 hover:text-gray-600 underline">取消修改</button>}</h3>
                 <form onSubmit={saveTransaction} className="space-y-8">
                  <div className="grid grid-cols-1 md:grid-cols-6 gap-6 items-end">
                    <div><label className="text-xs text-gray-400 block mb-2 font-black uppercase">日期</label><input type="date" className="w-full p-4 border rounded-xl outline-none focus:ring-2 focus:ring-blue-100 bg-white" value={newTx.date} onChange={e => setNewTx({...newTx, date: e.target.value})} /></div>
                    <div><label className="text-xs text-gray-400 block mb-2 font-black uppercase">類型</label><select className="w-full p-4 border rounded-xl outline-none focus:ring-2 focus:ring-blue-100 bg-white" value={newTx.type} onChange={e => setNewTx({...newTx, type: e.target.value, category: CATEGORIES[e.target.value][0]})}>{/* Options */}<option value="expense">支出</option><option value="income">收入</option></select></div>
                    <div><label className="text-xs text-blue-500 font-bold block mb-2 font-black uppercase tracking-widest">收支歸屬</label><select className="w-full p-4 border border-blue-100 rounded-xl bg-blue-50/20 font-bold outline-none" value={newTx.linkedType || "general"} onChange={e => setNewTx({...newTx, linkedType: e.target.value, linkedId: null})}><option value="general">一般專案收支</option><option value="land">土地出售人</option><option value="building">建物出售人</option></select></div>
                    {newTx.linkedType !== 'general' && (<div><label className="text-xs text-blue-500 font-bold block mb-2 font-black uppercase tracking-widest">具體對象</label><select className="w-full p-4 border border-blue-100 rounded-xl bg-blue-50/20 font-bold outline-none" value={newTx.linkedId || ""} onChange={e => setNewTx({...newTx, linkedId: Number(e.target.value)})}>{/* Options */}{newTx.linkedType === 'land' ? lands.map(l=><option key={l.id} value={l.id}>{l.sellers.map(s=>s.name).join('/')}</option>) : buildings.map(b=><option key={b.id} value={b.id}>{b.sellers.map(s=>s.name).join('/')}</option>)}</select></div>)}
                    <div><label className="text-xs text-gray-400 block mb-2 font-black uppercase">會計科目</label><select className="w-full p-4 border rounded-xl outline-none focus:ring-2 focus:ring-blue-100 bg-white" value={newTx.category} onChange={e => setNewTx({...newTx, category: e.target.value})}>{CATEGORIES[newTx.type].map(c=><option key={c} value={c}>{c}</option>)}</select></div>
                    <div><label className="text-xs text-blue-600 block mb-2 font-black underline uppercase">金額 ($)</label><input type="number" className="w-full p-4 border border-blue-100 rounded-xl font-black bg-blue-50/30 outline-none" value={newTx.amount} onChange={e => setNewTx({...newTx, amount: e.target.value})} /></div>
                  </div>
                  <div className="grid grid-cols-1 md:grid-cols-12 gap-6 items-center">
                    <div className="md:col-span-8"><input placeholder="備註說明..." className="w-full p-4 border rounded-xl outline-none focus:ring-2 focus:ring-blue-100 bg-gray-50 focus:bg-white" value={newTx.note} onChange={e => setNewTx({...newTx, note: e.target.value})} /></div>
                    <div className="md:col-span-2 relative"><input type="file" id="fileUploadGlobal" className="hidden" accept="image/*" onChange={handleImageUpload} /><label htmlFor="fileUploadGlobal" className={`flex justify-center items-center gap-2 w-full p-4 border-2 border-dashed rounded-xl text-xs font-black cursor-pointer transition-all hover:bg-blue-50 ${newTx.image ? 'bg-blue-50 border-blue-400 text-blue-600' : 'bg-gray-50 border-gray-200 text-gray-400'}`}>{newTx.image ? <><Check className="w-5 h-5" /> 單據已存</> : <><Camera className="w-5 h-5" /> 插入圖片</>}</label>{newTx.image && <button type="button" onClick={(e) => { e.preventDefault(); setNewTx({...newTx, image: null}); }} className="absolute -top-2 -right-2 bg-red-500 text-white rounded-full p-1.5 shadow-lg hover:bg-red-600 transition z-10"><X className="w-3 h-3" /></button>}</div>
                    <div className="md:col-span-2"><button type="submit" className={`w-full py-4 rounded-xl text-white text-sm font-black shadow-xl transition-all ${editingTxId ? 'bg-orange-600 hover:bg-orange-700' : 'bg-blue-600 hover:bg-blue-700 uppercase tracking-widest'}`}>{editingTxId ? "更新" : "錄入"}</button></div>
                  </div>
                 </form>
              </div>
           </div>
        )}
      </div>

      {/* 列印報表 (隱藏區) */}
      <div className="hidden print:block print:p-8">
        <h1 className="text-2xl font-bold mb-2">專案管理報表: {projectName}</h1>
        <p className="text-sm text-gray-500 mb-8">列印日期: {new Date().toLocaleDateString()}</p>
        
        {printConfig.team && <section className="mb-8 break-inside-avoid"><h2 className="text-lg font-bold border-b pb-2 mb-4">一、專案團隊資訊</h2><div className="grid grid-cols-2 gap-4 border p-4"><div>仲介公司: {projectTeam.agency}</div><div>經紀人: {projectTeam.broker}</div><div>開發業務: {projectTeam.developer}</div><div>行銷業務: {projectTeam.marketer}</div><div>承辦代書: {projectTeam.scrivener}</div></div></section>}
        {printConfig.lands && <section className="mb-8 break-inside-avoid"><h2 className="text-lg font-bold border-b pb-2 mb-4">全案土地總結算</h2><div className="grid grid-cols-3 gap-4 border p-4 text-center"><div><span className="block text-xs text-gray-500 font-bold">總面積 (㎡)</span><span className="text-xl font-black">{landGrandTotal.m2}</span></div><div><span className="block text-xs text-gray-500 font-bold">總坪數</span><span className="text-xl font-black">{landGrandTotal.ping}</span></div><div><span className="block text-xs text-gray-500 font-bold">總金額 ($)</span><span className="text-xl font-black">${Number(landGrandTotal.price).toLocaleString()}</span></div></div></section>}
        {printConfig.buyers && <section className="mb-8"><h2 className="text-lg font-bold border-b pb-2 mb-4">買受人資訊</h2><table className="w-full text-sm border-collapse border border-gray-300"><thead><tr className="bg-gray-100"><th className="border p-2">姓名</th><th className="border p-2">電話</th><th className="border p-2">地址</th></tr></thead><tbody>{buyers.map(b => (<tr key={b.id}><td className="border p-2">{b.name}</td><td className="border p-2">{b.phone}</td><td className="border p-2">{b.address}</td></tr>))}</tbody></table></section>}
        {printConfig.lands && <section className="mb-8"><h2 className="text-lg font-bold border-b pb-2 mb-4">土地標的</h2><table className="w-full text-sm border-collapse border border-gray-300"><thead><tr className="bg-gray-100"><th className="border p-2">出售人</th><th className="border p-2">地段</th><th className="border p-2">地號</th><th className="border p-2">面積(m2)</th><th className="border p-2">坪數</th><th className="border p-2">總金額</th></tr></thead><tbody>{lands.map(l => (<tr key={l.id}><td className="border p-2">{l.sellers.map(s => s.name).join(', ')}</td><td className="border p-2">{l.section}</td><td className="border p-2 text-xs">{l.items.map(i => i.lotNumber).join(', ').substring(0, 50)}</td><td className="border p-2">{Number(l.holdingAreaM2).toFixed(3)}</td><td className="border p-2">{Number(l.holdingAreaPing).toFixed(3)}</td><td className="border p-2">${Number(l.totalPrice).toLocaleString()}</td></tr>))}</tbody></table></section>}
        {printConfig.buildings && <section className="mb-8"><h2 className="text-lg font-bold border-b pb-2 mb-4">建物標的</h2><table className="w-full text-sm border-collapse border border-gray-300"><thead><tr className="bg-gray-100"><th className="border p-2">出售人</th><th className="border p-2">建照</th><th className="border p-2">地址</th><th className="border p-2">建號</th><th className="border p-2">面積(m2)</th><th className="border p-2">總金額</th></tr></thead><tbody>{buildings.map(b => (<tr key={b.id}><td className="border p-2">{b.sellers.map(s => s.name).join(', ')}</td><td className="border p-2">{b.permitNumber}</td><td className="border p-2">{b.address}</td><td className="border p-2">{b.buildNumber}</td><td className="border p-2">{b.areaM2}</td><td className="border p-2">${Number(b.totalPrice).toLocaleString()}</td></tr>))}</tbody></table></section>}
        {printConfig.handover && handoverData && (<section className="mb-8 break-inside-avoid"><h2 className="text-lg font-bold border-b pb-2 mb-4">交屋點交確認</h2><div className="grid grid-cols-2 gap-2 text-sm border p-4"><div>點交日期: {handoverData.handoverDate}</div><div>遙控器: {handoverData.remotes}</div><div>小門鑰匙(前): {handoverData.keysFront}</div><div>小門鑰匙(後): {handoverData.keysBack}</div><div>保固書: {handoverData.warranty?"有":"無"}</div><div>竣工圖: {handoverData.drawings?"有":"無"}</div><div>使照正本: {handoverData.originalPermit?"有":"無"}</div><div>電單號碼: {handoverData.electricityBill}</div><div>水單號碼: {handoverData.waterBill}</div></div></section>)}
        
        {printConfig.finance && (
          <section>
            <h2 className="text-lg font-bold border-b pb-2 mb-4">收支明細</h2>
            <div className="space-y-4">
               {['general', 'land', 'building'].map(type => {
                  if (!visibleLedgers[type]) return null;
                  const subData = groupedTransactions[type];
                  if(subData.length === 0) return null;
                  const label = type==='general' ? '一般專案收支' : type==='land' ? '土地出售人帳目' : '建物出售人帳目';
                  const subStats = stats.subTotals[type];
                  const net = (subStats?.income || 0) - (subStats?.expense || 0);

                  return (
                    <div key={type} className="break-inside-avoid mb-4">
                      <h3 className="font-bold text-sm bg-gray-100 p-1 mb-1">{label}</h3>
                      <table className="w-full text-xs border-collapse border border-gray-300">
                        <thead><tr className="bg-gray-50"><th className="border p-2">日期</th><th className="border p-2">類型</th><th className="border p-2">對象/備註</th><th className="border p-2 text-right">金額</th></tr></thead>
                        <tbody>{subData.map(t => (<tr key={t.id}><td className="border p-2">{t.date}</td><td className="border p-2">{t.category}</td><td className="border p-2">{t.note}</td><td className="border p-2 text-right">${Number(t.amount).toLocaleString()}</td></tr>))}</tbody>
                        {/* ✅ 列印專用小計 */}
                        <tfoot className="bg-gray-50 border-t-2 border-gray-300 font-bold">
                            <tr>
                                <td colSpan="3" className="border p-2 text-right">本欄小計</td>
                                <td className="border p-2 text-right">
                                   <div>收: ${(subStats?.income||0).toLocaleString()}</div>
                                   <div>支: ${(subStats?.expense||0).toLocaleString()}</div>
                                   <div className="border-t border-gray-400 mt-1 pt-1">淨: ${net.toLocaleString()}</div>
                                </td>
                            </tr>
                        </tfoot>
                      </table>
                    </div>
                  )
               })}
            </div>
          </section>
        )}
      </div>

      {/* 圖片預覽 Modal (略) */}
      {previewImage && (
        <div className="fixed inset-0 bg-black/90 backdrop-blur-md z-[9999] flex items-center justify-center p-6 print:hidden animate-fadeIn" onClick={() => setPreviewImage(null)}>
          <div className="relative w-full max-w-4xl h-full flex flex-col justify-center">
            <button onClick={() => setPreviewImage(null)} className="absolute top-0 right-0 text-white bg-white/10 p-3 rounded-full hover:bg-white/20 transition shadow-2xl mb-4"><X className="w-8 h-8" /></button>
            <img src={previewImage} alt="預覽" className="max-w-full max-h-[85vh] rounded-2xl shadow-2xl mx-auto object-contain bg-white" />
          </div>
        </div>
      )}
    </div>
  );
};

export default ProjectEditor;